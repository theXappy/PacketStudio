<UserControl x:Class="PacketStudio.NewGUI.PacketTemplatesControls.SctpTemplateControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PacketStudio.NewGUI.PacketTemplatesControls"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="350">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" MinWidth="80"/>
            <ColumnDefinition Width="8*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Label DockPanel.Dock="Left" MinWidth="60">Stream:</Label>
        <TextBox x:Name="streamTextbox" Grid.Column="1" TextChanged="StreamTextbox_OnTextChanged"/>
        <Label DockPanel.Dock="Left"  Grid.Row="1" MinWidth="60">Link Layer:</Label>
        <ComboBox x:Name="ppidBox" Grid.Column="1" Grid.Row="1" SelectionChanged="ppidBox_SelectionChanged" />
    </Grid>

</UserControl>
